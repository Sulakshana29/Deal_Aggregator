apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: deal-aggregator
  labels:
    app: backend
type: Opaque
# ⚠️  LEARNING NOTE: These values are base64-encoded (NOT encrypted).
# K8s Secrets are only as safe as the cluster's RBAC configuration.
# For Phase 5 (Security Hardening), we'll use:
#   - Sealed Secrets (bitnami/sealed-secrets), OR
#   - External Secrets Operator with a real secret store
#
# To generate your own base64 value:
#   echo -n 'your-value' | base64
#
# Current values below are placeholders — replace before deploying to production!
data:
  # placeholder: mongodb://mongodb:27017/deal-aggregator
  MONGO_URI: bW9uZ29kYjovL21vbmdvZGI6MjcwMTcvZGVhbC1hZ2dyZWdhdG9y
